use axum::Router;

pub static DEFAULT_NEST_ROUTE: &str = "/";

/*
pub trait SectorLogger {
    fn log(message: usize) {
        //registered {} partial sector(s) for IAM"
        println!("{} {}" , "[ARC]", message)
    }
}
*/